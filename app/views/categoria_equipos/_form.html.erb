<%= form_for(@categoria_equipo) do |f| %>
  <% if @categoria_equipo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categoria_equipo.errors.count, "error") %> prohibited this categoria_equipo from being saved:</h2>

      <ul>
      <% @categoria_equipo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
